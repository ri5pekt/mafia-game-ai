#
# Local environment variables for mafia-game-ai
#
# Copy to ".env" in the repo root and fill in values.
# Never commit real secrets.
#

# OpenAI
OPENAI_API_KEY=

# Optional: default model for /ai/act (if request.model not provided)
# OPENAI_MODEL=gpt-5-nano

# Google Cloud Text-to-Speech
# Set this to the path of your service account JSON key.
# For Docker Compose, we mount ./keys into the api container and point to /workspace/keys/<file>.json.
# GOOGLE_APPLICATION_CREDENTIALS=keys/text-to-speech-XXXX.json


